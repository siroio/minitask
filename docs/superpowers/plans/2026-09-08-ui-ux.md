# UI/UX Implementation Plan

User approved `docs/research/2026-09-08-ui-ux.md` in chat. Execute inline in the current workspace; no commit or push.

**Goal:** Compact Japanese TUI with smart views, quick add, live search, command/help menus, date presets and safe one-step undo.

**Architecture:** Keep Store/Markdown/index/editor; extend Store with in-session undo. Replace the oversized UI's layout and event handling, keeping rendering in `src/ui/render.rs` and a shared small text input in `src/ui.rs`. Use existing Ratatui/time dependencies.

- [x] Storage: add failing undo/source-preservation tests, then implement guarded undo for completion/date changes. Validate form dates before a single task append.
- [x] Views: add real App tests for today/upcoming/unscheduled/all/completed, restored view, live search cancellation and compact 80x24 rendering. Keep workspace scope explicit.
- [x] Input: implement quick add with title/workspace/scheduled/due fields, searchable commands/help, direct dates and presets. Preserve drafts on validation errors and picker identity across refresh.
- [x] Rendering: one-line rows, Japanese labels, responsive sidebar/details, distinct calendar counts, source-safe action routing and feedback.
- [x] Verify all workflows, add timing measurement for 10000 tasks, review source safety, exercise Yazi and real editors, update README and replace executable when idle.

Constraints: No emoji or new storage migration. Preserve Yazi Ctrl+T, old Markdown compatibility and Neovim/Emacs. Undo must refuse external changes. No speculative board/status model or new services.

## Verification result

- 26 automated tests passed; two manual benchmarks are ignored in the normal suite. Clippy and formatting passed.
- New checks cover smart-view boundaries, form validation without partial writes, calendar quick add, presets, live search cancellation, selection restoration after external removal, small help scrolling, midnight refresh, and guarded undo.
- 10000 dated tasks / 120x30 / 11-run median: render 0.29ms, search+render 3.68ms, calendar navigation+render 0.37ms. TestBackend timings exclude terminal transport. Before viewport rendering, the same render benchmark took 10.32ms.
- Terminal: add with dates, live search, completion/date undo, Neovim edit/save/return and Emacs edit/save/return verified using isolated sample Markdown.
- Review findings fixed: hidden detail navigation, date-help routing, local-day refresh, help scrolling, and stale search selection after external deletion.
- Installed executable: minitask 0.3.0. Previous executable kept at .cache/minitask-before-ux.exe. No commit or push.
- Final installed binary: Yazi Ctrl+T launch, live search, command palette and return to Yazi verified. Root/release SHA256 matched.
